// Injects language strings into the site
function injectLanguage(language) {
  console.log("Injecting language:", language);
  // Example: could replace placeholders in HTML with actual language strings
}

// Expose globally
window.injectLanguage = injectLanguage;

